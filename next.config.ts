import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['samehadaku.mba', 'imgsrv.crunchyroll.com', 'arank-party-ridatsu-official.com', 'a.storyblok.com', 'v1.samehadaku.how', 'v2.samehadaku.how'],
  },
};

export default nextConfig;
